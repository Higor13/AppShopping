<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:AppShopping.ViewModels"
             x:Class="AppShopping.Views.Wifi">

    <Shell.TitleView>
        <ContentView>
            <Image
                HeightRequest="30"
                HorizontalOptions="Start"
                Source="TitleViewLogo"
                VerticalOptions="Center" />
        </ContentView>
    </Shell.TitleView>

    <ContentPage.BindingContext>
        <viewModels:WifiViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout Padding="10, 10, 10, 0">
            <Label Text="Novidades" Style="{StaticResource TitleType}" />
            <Label Text="Para você" Style="{StaticResource TitleName}" />

            <Frame Padding="10,0" Margin="0, 40" BorderColor="{StaticResource BorderColor}" CornerRadius="0" HasShadow="False">
                <Entry Placeholder="E-mail" Text="{Binding Email}" Style="{StaticResource EntryPayment}" />
            </Frame>

            <Button Text="Conectar e liberar wifi" Style="{StaticResource TicketPaidButton}" Command="{Binding ConnectToWifiCommand}" HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>